// push():
 /* Add one or more items at the array.
    Item will be added at the end.
    change the lenght of array. */
    // Example
    let fruit = ["Apple","Banana","Orange","Grapes"];
    console.log(fruit);
    fruit.push("Kiwi","Blueberry");
    console.log(fruit);
 // pop():

 /* Removes the last items from the en d.
    Returns the removed element.
    change the lenght of array. */
    // Example
     let Number = ["20","30","40","50"];
    console.log(Number);
      Number.pop();
    console.log(Number); 

    //shift():it is used to remove the first element from an array and returns that element.
//Example
let book = ["target", "faith", "succes", "failure"];
console.log(book);
book.shift();
console.log(book); 

//unshift():it is used to add an element to the beginning of an array.

//Example

 let bird = ["pigeon", "peacock", "parrot", "duck"];
console.log(bird);
fruit.unshift("parrot");
console.log(bird);